$width-el = 10vw
$height-el = 10vw
$depth-el = 10vw
$white-1 = #f2f3ed;
$white-2 = #dbdbdb;
$white-3 = #b5b8bf;
$white-4 = #aaaaaa; 

*
*:before
*:after
    box-sizing: border-box
    transform-style: preserve-3d

body
    display flex
    align-items center
    justify-content: center
    background-color: #d7385d;

    color: white;
    font-family: monospace;
    font-size: 0.8vw;
    letter-spacing: 0.1vw;
    font-weight: bold;
    text-transform: uppercase;h

cube($width, $height, $depth)
    

.egg
    height calc(100 * var(--unit))
    width calc(90 * var(--unit))
    z-index 2
    position relative

    *
        position: absolute

    &__shell
        top 50%
        left 50%
        transform translate(-50%, -50%)
        height: 95%
        width: 74%

        &::after
        &::before
            content ''
            border: calc(2 * var(--unit)) solid var(--shell-outline)
            position: absolute
            background var(--shell)

        &::before
            top 0
            left 50%
            height 65%
            width: 100%
            transform: translate(-50%, 0%)
            border-radius: 50% 50% 0 0 / 100% 100% 0 0 
            $clip = inset(0 0 10% 0)
            -webkit-clip-path $clip
            clip-path $clip

        &::after
            bottom: 0
            width: 100%
            height: 50%
            border-radius: 0 0 50% 50% / 0 0 78% 78%
            $clip = inset(15% 0 0 0)
            -webkit-clip-path $clip
            clip-path $clip

    &__glasses
        height 31%
        width: 95%
        top: 31%
        left 50%
        transform translate(-50%, 0)

    &__glass
        top 0
        width: 46%
        height 95%
        transform: rotate(calc(var(--rotate) * 12deg)) rotateY(calc(var(--flip) * 180deg))
        border calc(3 * var(--unit)) solid var(--glasses)
        border-radius: 25% 60% 32% 38%/34% 37% 45% 30%

        &:after
            content ''
            position absolute
            height calc(100% + 6 * var(--unit))
            width calc(100% + 6 * var(--unit))
            top calc(-3 * var(--unit))
            left calc(-3 * var(--unit))
            border calc(3 * var(--unit)) solid var(--glasses)
            border-radius 25% 60% 32% 38%/34% 37% 45% 30%
            box-shadow calc(0 * var(--unit)) calc(3 * var(--unit)) calc(2 * var(--unit)) hsla(0, 0%, 0%, 0.45) inset, calc(0 * var(--unit)) calc(3 * var(--unit)) calc(2 * var(--unit)) hsla(0, 0%, 0%, 0.45)
            $clip = polygon(49% 0, 100% 0, 100% 160%, 90% 160%, 11% 150%, 17% 95%)
            -webkit-clip-path $clip
            clip-path: $clip

        &--left
            --flip 0
            --rotate -1
            left 0

        &--right
            --flip 1
            --rotate 1
            right 0

    &__glass-bridge
        height: 64%
        width: 25%
        top: 1%
        left 50%
        transform: translate(-50%)
        border-radius: 50% /50%
        border: calc(4 * var(--unit)) solid var(--glasses)
        $clip = polygon(10% 0, 90% 0, 80% 50%, 20% 50%)
        clip-path: $clip

    &__glasses-arm
        height: 14%
        width: 90%
        background: var(--glasses)
        left 50%
        top 35%
        transform: translate(-50%, 0)
        $clip = polygon(-7% 0, 25% 55%, 75% 55%, 107% 0, 100% 79%, 76% 100%, 24% 100%, 0 79%)
        clip-path: $clip
    

        
