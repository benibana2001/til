{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:pug": "npx pug src/pug/ --hierarchy -o dist/ -w -P",
    "build": "npx pug src/pug/ -o ./ -P"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pug": "^3.0.0"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}
