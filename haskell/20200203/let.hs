some = let
        x = 10
        y = 15 in
        x + y

something = x + y
    where
    x = 9
    y = 20

area :: Double -> Double
area x = c + s
    where
    c = (x / 2) ^ 2 * pi / 2
    s = x ^ 2
